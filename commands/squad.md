---
description: Start a named coworker squad on the grok-talks office bus
argument-hint: "[roles] [cwd]"
---

Call `talks_squad_start` with the roles the user named (comma-separated). Default is the full set: planner, explorer, frontend, backend, qa, validator, adversarial, security. Then call `talks_board` and show who joined.
