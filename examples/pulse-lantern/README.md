# Pulse Lantern

A tiny neon sign built by a grok-talks squad. The lead starts planner, explorer, frontend, backend, QA, validator, adversarial, and security on the same office bus. They talk, collide on `sign.html`, then stitch `index.html`.

```bash
npx vitest run tests/examples/pulse-lantern.test.ts
open examples/pulse-lantern/out/index.html
```
