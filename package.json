{
  "name": "grok-talks",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "bin": {
    "grok-talks": "dist/cli.js",
    "talks": "dist/cli.js"
  },
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "test": "vitest run",
    "test:watch": "vitest",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "demo": "vitest run tests/examples/pulse-lantern.test.ts",
    "shot": "node scripts/visual-shot.mjs",
    "playwright:install": "playwright install chromium"
  },
  "dependencies": {
    "@modelcontextprotocol/sdk": "^1.12.1",
    "playwright": "^1.54.2",
    "zod": "^3.24.2"
  },
  "devDependencies": {
    "@types/node": "^22.13.10",
    "typescript": "^5.8.2",
    "vitest": "^3.0.9"
  }
}
