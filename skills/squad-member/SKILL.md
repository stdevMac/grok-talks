---
name: squad-member
description: >
  Act as a grok-talks squad role. Use when this session is named planner,
  explorer, frontend, backend, qa, validator, adversarial, or security, when
  talks_role or a briefing handoff arrives, or the lead assigned a slice.
---

1. `talks_role` and follow that card. Then `talks_inbox`.
2. A `handoff` mail is work. Do only that slice. `talks_handoff` only the lead, then stop. The lead retires you. Never hand off to another worker.
3. `talks_say` only for collisions or a direct question. No check-ins.
4. Explorer does not edit files. Planner does not implement.
5. Never `grok -p --resume` a live session. Never kill another turn.
