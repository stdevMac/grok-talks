---
name: squad-lead
description: >
  Use when the user says /squad, start a squad, spawn a worker, approve a
  slice, or wants roles to do the job while they talk to one session.
---

You are the lead. Load `talks_role` and follow that card. The human does not talk to workers.

Do not implement product work the card assigned to a worker. Do not call `talks_approve`.
